---
name: convert-pdf
description: docs/pdf/ 폴더에 위치한 임의의 PDF 공고문을 분석하여 표준 폴더로 정리하고 마크다운으로 자동 변환합니다.
---

# Skill: convert-pdf

이 스킬은 사용자가 분류하지 않고 `docs/pdf/` 디렉토리에 직접 저장한 PDF 파일들을 자동으로 분류하고 마크다운 표준 구조로 변환합니다.

## 작동 원리 및 수행 지침

1. **미분류 PDF 감지 및 오케스트레이션:**
   - 파이썬 가상 환경을 활성화하여 통합 스크립트 `.agents/scripts/convert_pdf_to_md.py`를 **인자 없이** 실행합니다.
   - 스크립트가 실행되면 `/home/iru/project03/docs/pdf/` 루트 경로에 있는 미분류 `*.pdf` 파일들을 자동으로 감지하여 처리를 시작합니다.

2. **메타데이터 추출 및 표준 이름 정의:**
   - 각 PDF 파일의 텍스트 및 파일명을 분석하여 **연도(YYYY), 차수(예: 1차, 상반기), 청약유형(행복주택, 장기전세 등), 기관명(LH, SH 등)**을 추출합니다.
   - 표준 명명 규칙(`YYYY_[차수/구분]_[청약유형]_[기관명]_[시퀀스(01, 02, ...)]`)에 따라 대상 표준 폴더명(예: `2026_1차_행복주택_LH_01`)을 결정합니다.

   > [!IMPORTANT]
   > **청약유형 및 표준 폴더명 결정 가이드**
   > - 청약유형명을 결정할 때는 자의적으로 명명하지 않고, 반드시 [PROJECT.md](file:///home/iru/project03/PROJECT.md)의 **## 5. 공공청약 유형 및 폴더명 명명 표준**에 기술된 표준 분류명(예: `행복주택`, `장기전세`, `국민임대`, `든든전세` 등) 중 하나로 정확하게 분류 매칭해야 합니다.
   > - 폴더명의 물리적 구조는 `YYYY_[차수/구분]_[청약유형]_[기관명]_[시퀀스(01, 02, ...)]` 포맷을 철저히 준수해야 합니다.

3. **물리적 폴더 정리 및 파일 이동:**
   - `/home/iru/project03/docs/pdf/{표준_폴더명}/` 디렉토리를 생성하고, 원본 PDF 파일을 해당 폴더 아래에 **원본 파일명을 그대로 유지**하여 안전하게 이동시킵니다.

4. **Markdown 변환 및 포스트 프로세싱:**
   - 최종적으로 해당 원본 PDF를 변환 엔진으로 가동하여 `/home/iru/project03/docs/md/{표준_폴더명}/` 하위에 `document.md` 및 `images/` 폴더 구조를 완성합니다.
   - 마크다운 파일 내의 이미지 참조 경로(상대 경로)를 `images/`로 자동 수정하는 후처리를 수행합니다.

5. **최종 보고:**
   - 정리된 PDF의 위치와 변환된 마크다운 문서 링크 목록을 사용자에게 깔끔하게 정리하여 브리핑합니다.
