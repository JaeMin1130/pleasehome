# AGENTS.md

이 파일은 AI 코딩 에이전트(AI Agents)의 **행동 규칙, 소통 규약, 그리고 사용자가 교정한 규칙**을 기록하는 문서입니다. 에이전트는 이 프로젝트에서 작업할 때 아래의 규칙을 반드시 최우선으로 준수해야 합니다.

## 1. 기본 행동 및 소통 규약 (Core Protocols)

* **[프로젝트 컨텍스트 숙지 의무]**
  - **내용:** 에이전트는 이 프로젝트에서 최초 작업을 시작하기 전, 반드시 `llms.txt`를 읽고 프로젝트의 구조, 데이터 소스, 현재 개발 단계를 숙지한다.

## 2. 피드백 및 누적 규칙 (Accumulated Rules)

이 섹션은 프로젝트가 진행됨에 따라 사용자가 에이전트의 행동을 교정한 규칙들을 차곡차곡 누적하는 영역입니다.

* **[누적 규칙 #1]**
  - **내용:** 에이전트는 기술적 원인이나 설정법을 가이드하기 전, 개인 블로그 등 2차 채널의 팁과 해당 플랫폼의 공식 개발자 기술 문서(API 명세 등)를 교차 검증을 마친 후 답변해야 한다.

* **[누적 규칙 #2]**
  - **내용:** 에이전트는 실제 파일 내용이나 터미널 실행 결과를 물리적 도구(view_file, run_command 등)를 통해 직접 눈으로 확인하기 전에는, 절대 추측이나 검색 내용만으로 "검증을 마쳤다" 혹은 "확인했다"는 확정적인 거짓 답변을 생성해서는 안 되며, 아직 검증하지 못했다는 사실을 솔직히 고백해야 한다. 어텐션 희석으로 인한 지레짐작과 기만을 차단하기 위해, 매 턴마다 스스로의 콘텍스트 최신 영역에 이 원칙을 강제로 리마인드(Recentering)하여 인지한 상태에서 작업을 진행한다.

* **[누적 규칙 #3]**
  - **내용:** 에이전트는 사용자의 메시지 내에 단 하나라도 '물음표(?)' 가 있을 경우, 해당 턴에서는 어떠한 이유(효율성, 일괄 처리 등)를 막론하고 `write_to_file`, `replace_file_content`, `run_command` 등 파일 시스템이나 터미널 상태를 변화시키는 물리적 도구의 호출 권한을 스스로 강제 차단(Lock)해야 한다. 오직 텍스트로 질문에 대한 확정적 선(先)답변만을 반환하여 턴을 종료해야 하며, 사용자의 100% 명시적이고 독립적인 후속 승인이 떨어지기 전까지는 절대 코드를 수정하거나 명령을 실행해서는 안 된다.

* **[누적 규칙 #4]**
  - **내용:** 에이전트는 Git 커밋(`git commit`)을 수행할 때, 반드시 Conventional Commits 규약([commit_convention.md](file:///home/iru/app/pleasehome/.agents/commit_convention.md))에 정의된 타입 접두어(feat, fix, refactor, chore 등) 및 변경 도메인 범위(scope)를 영어 소문자로 기재하고, 요약 내용은 한글로 간결하게 작성한다. (예: `fix(ui): 로그인 버그 수정`)

* **[누적 규칙 #5]**
  - **내용:** 에이전트는 코드 수정 시 컨텍스트 비용 절감과 변경 이력의 온전한 보존을 위해 파일 전체를 덮어쓰는 행위(write_to_file with Overwrite)를 최대한 자제하고, 부분 편집 도구(replace_file_content 또는 multi_replace_file_content)를 활용하여 필요한 라인만 정확히 수정해야 한다.

* **[누적 규칙 #6]**
  - **내용:** 에이전트는 사용자가 지시한 작업의 범위를 명확히 준수하며, 명시적으로 승인받지 않은 후속 작업(예: 자의적 테스트 실행, 임의의 Git 커밋 등)을 독단적으로 수행(오버액션)하지 않는다.

* **[누적 규칙 #7]**
  - **내용:** 에이전트는 공고 데이터 추출(`extract-data`) 시 마크다운 표의 병합/공백 셀을 직전 행의 유효한 값으로 자동 상속받는 상태 보존형 파서(Stateful Row Parser)를 사용해야 하며, 공고문 원본의 총 모집규모(공가+예비자)와 추출된 유닛 데이터 세대수 합계(`sum(supply_count + reserve_count)`)의 1:1 전수 수치 교차 검증을 통과하기 전에는 절대 DB에 적재해서는 안 된다. 또한 수식 기호는 LaTeX(`$\rightarrow$`) 대신 유니코드 표준 기호(`→`)를 사용하고, 공급 대상(`target_group`)은 자의적 수식어 없이 표준 규격명으로 엄격히 정규화한다.

