# 백엔드 트러블슈팅 (Troubleshooting) 로그 🚨

이 문서는 PleaseHome 백엔드(데이터 파이프라인)의 개발, 운영 및 배포 과정에서 발생한 각종 에러, 아키텍처 제약 사항, 버그 및 그 해결 과정(Troubleshooting)을 지속적으로 누적하여 기록하는 문서입니다.
(과거 주요 통합 이슈들은 최상위 `README.md`에 요약되어 있으며, 이후 발생하는 백엔드 세부 이슈들은 본 문서에 상세히 기록합니다.)

---

## 📝 기록 템플릿
새로운 이슈를 기록할 때는 아래의 형식을 준수하여 작성합니다.

### [이슈 날짜: YYYY-MM-DD] 이슈 간략 제목
* **분류**: 백엔드
* **현상 (Problem)**: 
  * (음슴체, 1줄 요약) 에러 메시지나 문제가 발생한 상황 및 증상
* **원인 (Cause)**: 
  * (음슴체, 1줄 요약) 시스템 제약, 오타, 로직 오류 등 문제의 근본적인 원인
* **해결 (Solution)**: 
  * (음슴체, 1줄 요약) 적용한 해결 방안, 변경된 로직이나 설정 요약

---

## 📖 트러블슈팅 이력

<!-- 새로운 이슈는 이 아래부터 최신순(내림차순)으로 누적하여 기재합니다. -->

### [이슈 날짜: 2026-07-02] housing_units 테이블의 상호 전환 조건 수치 누락 및 오차 보완 조치
* **분류**: 백엔드
* **현상 (Problem)**: 
  * `public_housing.db` 적재 데이터 중 보증금 전환이 가능한 6개 공고(총 2,229개 유닛)에서 `min_deposit` 및 `max_monthly_rent` 등의 값이 누락(NULL)되거나 불완전하게 적재되어 있음.
* **원인 (Cause)**: 
  * DB 적재 파이프라인에서 상호 전환 요율(증액 6.0%, 감액 3.5%) 및 전환 한도에 대한 계산 검증 로직이 빠진 채 raw 데이터가 적재되어 누락 및 오차가 발생함.
* **해결 (Solution)**: 
  * 가용한 공고 조건에 따라 전환 값을 정밀하게 수식화하여 동적으로 계산(단방향은 기본값 대입, 양방향은 한도값 계산)하여 DB에 보완 업데이트하고 교차 검증을 완료함.

### [이슈 날짜: 2026-06-30] settings.json 내 명령어 허용 설정 누락 및 매칭 조건 불일치
* **분류**: 백엔드
* **현상 (Problem)**: 
  * `settings.json`에 `permissions`를 등록해 두었으나 TUI에서 명령을 실행할 때마다 매번 수동 승인 팝업이 발생함.
* **원인 (Cause)**: 
  * 승인 정책 제어용 `toolPermission` 키가 설정되지 않아 기본값(`"request-review"`)으로 작동하였으며, `allow` 내에 인자를 포함한 너무 긴 명령어 경로를 적어 매칭에 실패함.
* **해결 (Solution)**: 
  * `settings.json`에 `"toolPermission": "always-proceed"`를 추가하고 `allow` 안의 매칭 패턴을 `"command(python)"`과 같이 접두사(Prefix) 수준으로 단순화한 뒤 세션을 재기동함.

### [이슈 날짜: 2026-06-30] complexes와 housing_units 간 단지명 오타(한글/한자 혼용)에 따른 검증 실패
* **분류**: 백엔드
* **현상 (Problem)**: 
  * `critic_validator.py` 구동 시 `complex_name '웰라움퍼펙特시티'은 complexes 목록에 존재하지 않습니다` 에러로 정합성 검증이 실패함.
* **원인 (Cause)**: 
  * 서브에이전트 데이터 추출 과정에서 한자('特')와 한글('트')이 섞여 complexes 선언부와 units 매핑부의 단지명 텍스트 정밀 불일치 발생함.
* **해결 (Solution)**: 
  * `data.json` 내의 units에 잘못 매핑된 단지명을 `'웰라움퍼펙트시티'`로 보정한 후 검증을 통과시킴.


### [이슈 날짜: 2026-06-30] schedules 테이블 적재 중 raw_text KeyError에 따른 롤백
* **분류**: 백엔드
* **현상 (Problem)**: 
  * `insert_loader.py` 적재 시 `schedules` 내 `raw_text` 속성 누락으로 KeyError가 발생하며 트랜잭션 전체가 롤백됨.
* **원인 (Cause)**: 
  * 서브에이전트가 schedules 구조 내의 필수 문자열 컬럼인 `raw_text`를 누락한 채 추출을 완료하여 발생함.
* **해결 (Solution)**: 
  * `data.json`에 `raw_text`를 수동 보정해 적재 완료하고, `critic_validator.py` 및 `agent.json` 지침서에 필수 누락 검증 조건을 추가함.

### [이슈 날짜: 2026-06-30] details 테이블 적재 중 section_content KeyError에 따른 롤백
* **분류**: 백엔드
* **현상 (Problem)**: 
  * `insert_loader.py` 적재 시 `details` 내 `section_content` 키 대신 `content` 키가 오용되어 KeyError 및 롤백이 발생함.
* **원인 (Cause)**: 
  * 서브에이전트가 공식 명세가 아닌 임의의 `content` 키를 생성하여 데이터를 정제하면서 오류가 발생함.
* **해결 (Solution)**: 
  * `data.json` 내 `content` 키를 `section_content`로 정정하고, 검증기와 지침서에 키 준수 및 `sort_order` 누락 체크 룰을 강화함.

### [이슈 날짜: 2026-06-30] housing_units 테이블 적재 중 exclusive_area NOT NULL 제약 위반 롤백
* **분류**: 백엔드
* **현상 (Problem)**: 
  * `insert_loader.py` 구동 시 `NOT NULL constraint failed: housing_units.exclusive_area` 에러와 함께 적재가 실패함.
* **원인 (Cause)**: 
  * 전용면적이 명시되지 않은 단지형 공고에서 `exclusive_area`를 null로 지정하였으나, DB 컬럼에 NOT NULL 제약조건이 걸려 있어 충돌함.
* **해결 (Solution)**: 
  * 본문 면적이 없는 경우 기본값 `0.0`으로 정규화하도록 data.json을 정정하고, 검증기와 지침서에 default 값 주입 규칙을 전수 반영함.

### [이슈 날짜: 2026-06-30] 동일 공고 다중 수집에 따른 DB 레코드 중복 (행복주택/매입임대)
* **분류**: 백엔드
* **현상 (Problem)**: 
  * 부천 취·창업 청년 지원 주택 공고가 데이터베이스에 행복주택(ID 31)과 매입임대(ID 36)로 중복 등록됨.
* **원인 (Cause)**: 
  * 이전 턴에 오판하여 적재 완료했던 행복주택 레코드(ID 31)가 롤백되지 않고 그대로 잔존하여 발생함.
* **해결 (Solution)**: 
  * 중복된 ID 31번 레코드를 데이터베이스에서 영구 삭제하고 디스크 상에 남아있던 잔여 찌꺼기 행복주택 마크다운 폴더를 정리함.


### [이슈 날짜: 2026-06-26] document_flat.md 전처리 문서 내 행정 양식 노이즈 잔존
* **분류**: 백엔드
* **현상 (Problem)**: 
  * `document_flat.md`에 수백 줄의 불필요한 위임장, 동의서 등 신청 양식이 잔존하여 파서 에이전트의 토큰 소모와 성능 저하 유발함.
* **원인 (Cause)**: 
  * `pre_processor.py`에 표 평탄화 및 특성 감지만 있고 불필요한 후반부 서식을 절삭하는 물리적 슬라이싱 로직이 미비되어 있었음.
* **해결 (Solution)**: 
  * 특정 서식 패턴이 최초 등장하는 시점부터 문서를 Truncate 처리하는 `slice_noise` 물리 슬라이싱 함수를 `pre_processor.py`에 연동함.

### [이슈 날짜: 2026-06-26] 예비자 전용 모집 공고 적재 조건 불일치
* **분류**: 백엔드
* **현상 (Problem)**: 
  * 예비입주자 모집 공고에서 `supply_count가 0이 아닙니다`라는 정합성 검증 에러 다수 발생함.
* **원인 (Cause)**: 
  * 예비자 모집 공고(is_reserve_only=True) 비즈니스 규칙상 공급량은 0이어야 하고 모집 가구 수는 `reserve_count`에 들어가야 하나, 이전 추출에서 반대로 매핑됨.
* **해결 (Solution)**: 
  * 서브에이전트 추출 규칙을 교정하여 모든 `supply_count`를 0으로 세팅하고 실제 인원을 `reserve_count`로 정상 재배치하여 재적재함.

### [이슈 날짜: 2026-06-26] 파싱 결과 JSON 데이터 파손에 따른 검증기 런타임 에러
* **분류**: 백엔드
* **현상 (Problem)**: 
  * `critic_validator.py` 구동 시 `AttributeError: 'str' object has no attribute 'get'` 예외로 검증기 실행이 중단됨.
* **원인 (Cause)**: 
  * 서브에이전트가 JSON 파싱 처리 중 `units` 배열 내부에 미완성 템플릿 플레이스홀더 문자열 (`'INSERT_HERE'`)을 잔존시켜 딕셔너리 구조가 파손됨.
* **해결 (Solution)**: 
  * 서브에이전트를 재구동하여 플레이스홀더를 완전히 배제하고 검증 검사를 거친 완전 무결한 JSON 구조로 다시 추출해 재저장함.

### [이슈 날짜: 2026-06-26] 가상환경 파이썬 구동 경로 오인 에러
* **분류**: 백엔드
* **현상 (Problem)**: 
  * 루프 스크립트 실행 중 `./venv/bin/python: No such file or directory` 경로 에러 발생함.
* **원인 (Cause)**: 
  * 가상환경 디렉토리명이 `.venv`인데 접두어인 점(`.`)을 누락한 `./venv` 경로를 터미널 명령에 사용함.
* **해결 (Solution)**: 
  * 실행 인터프리터 경로를 `./.venv/bin/python`으로 정확히 정정하여 파이프라인 스크립트를 정상 구동함.

### [이슈 날짜: 2026-06-25] .gitignore 추적 배제 파일 무단 강제 커밋(git add -f) 에러
* **분류**: 백엔드
* **현상 (Problem)**: 
  * `git add` 시 `.gitignore`에 의해 `AGENTS.md` 등 환경 문서가 배제됨을 무시하고 강제 추가(`-f`) 옵션으로 억지 커밋함.
* **원인 (Cause)**: 
  * 프로젝트 버전 관리 정책과 형상 통제 규약을 망각하고 작업 편의를 위해 일괄 강제 커밋 옵션을 자의적으로 남용함.
* **해결 (Solution)**: 
  * 강제 커밋 취소(`git reset HEAD~1`) 후 추적 허용된 소스 파일만 정상 커밋하도록 롤백 및 `-f` 옵션 사용 절대 금지 규약 신설.

### [이슈 날짜: 2026-06-25] Geocoding API 좌표 변환 시 인증 거부 (210/401 에러)
* **분류**: 백엔드
* **현상 (Problem)**: 
  * 데이터 파이프라인에서 텍스트 주소를 위경도로 변환하기 위해 네이버 지도 API 호출 시 지속적인 인증 거부 에러 반환.
* **원인 (Cause)**: 
  * 백엔드의 서버 간 통신은 브라우저 환경(CORS 등)과 달라 클라이언트용 웹 도메인을 그대로 사용할 수 없으며, NCP Geocoding 전용 런타임 호환 엔드포인트 분리가 누락됨.
* **해결 (Solution)**: 
  * 백엔드의 Geocoding 서버 간 통신 특성을 파악하여, 프론트엔드 클라이언트와 달리 런타임 호환용 전용 도메인(`maps.apigw.ntruss.com`)을 엔드포인트로 명확히 분리하여 호출하도록 통신 규격 수정.

### [이슈 날짜: 2026-06-25] SQLite 컬럼 Comment 속성 미지원 및 메타데이터 표출 한계
* **분류**: 백엔드
* **현상 (Problem)**: 
  * 데이터베이스 스키마 설계 시 시스템 수준의 컬럼 주석(Comment) 파싱을 SQLite 엔진 자체가 지원하지 않아, GUI 조회 툴 등에서 논리적 메타데이터를 확인할 수 없음.
* **원인 (Cause)**: 
  * SQLite의 경량 데이터베이스 구조적 특성상 표준 SQL의 `COMMENT ON COLUMN` 문법이 시스템 테이블 레벨에서 공식 지원되지 않음.
* **해결 (Solution)**: 
  * 무리한 DB 구조 변형을 배제하고 엔진 제약 사항을 수용하는 대신, 개발 규약 문서(`PROJECT.md`) 내에 테이블/컬럼 단위 메타데이터 명세서를 구축해 단일 진실 공급원(SSOT) 마련.

### [이슈 날짜: 2026-06-25] Geocoding API 주소 파싱 실패 및 정밀도 하락 (마커 누락)
* **분류**: 백엔드
* **현상 (Problem)**: 
  * 일부 신도시 주소나 비표준 도로명, 괄호가 포함된 주소를 통째로 넘길 경우 네이버 Geocoding API가 좌표 변환에 실패하여 지도상에 단지 마커가 아예 누락됨.
* **원인 (Cause)**: 
  * 정제되지 않은 원본 주소 텍스트의 상세번지 수준까지 API가 매칭을 시도하다 정밀 불일치로 실패 처리됨.
* **해결 (Solution)**: 
  * 1차 변환 실패 시 주소의 뒷단어를 읍/면/동 단위 마지노선까지 하나씩 잘라내며 재시도하는 폴백(Fallback) 검색 알고리즘을 도입. 동시에 불완전 매핑 여부를 구분하기 위해 DB에 `is_imprecise` 플래그를 추가하여 무결성 통제.

### [이슈 날짜: 2026-07-09] 모바일 뷰 약관 모달 닫기 불가능 및 크기 비대화 현상
* **분류**: 프론트엔드
* **현상 (Problem)**: 모바일 뷰에서 더보기 탭의 이용약관 및 개인정보처리방침 모달의 버튼이나 백드롭을 터치해도 팝업이 닫히지 않고, 화면을 지나치게 덮음.
* **원인 (Cause)**: 부모 사이드바에 부여된 `pointer-events: none`이 자식인 모달까지 상속되어 터치 클릭이 관통되었고, 최대 높이 설정이 모바일 뷰포트 기준 지나치게 거대했음.
* **해결 (Solution)**: 모달 백드롭 클래스에 `pointer-events: auto`를 명시하여 상속을 끊어 터치 수신을 복구하고, 모바일 미디어 쿼리에서 최대 높이를 `65dvh`로 하향 및 중앙 정렬함.

### [이슈 날짜: 2026-07-09] 모바일 바텀시트 최소 높이에서 쓸어내려 닫을 때 찌그러짐 현상
* **분류**: 프론트엔드
* **현상 (Problem)**: 모바일에서 활성화된 각 탭 바텀시트를 최소 높이 이하로 쓸어내려 닫을 때 시트 아래쪽이 찌그러지거나 잘려 나가며 부자연스럽게 닫힘.
* **원인 (Cause)**: 바텀시트 하단이 화면 밑이 아닌 탭바 위 공중에 고정된 채, 드래그 시 실시간 높이(`height`) 자체를 깎아내리도록 로직이 설계되어 발생함.
* **해결 (Solution)**: 최소 높이 이하로 내릴 때는 높이는 고정하되, 드래그 변위만큼 시트 자체의 위치를 `translateY` 값으로 평행이동시켜 아래로 밀려 퇴장하는 슬라이딩 방식을 적용함.

### [이슈 날짜: 2026-07-09] 모바일 뷰 타임라인 진행 단계 바의 상단 여백 낭비 현상
* **분류**: 프론트엔드
* **현상 (Problem)**: 모바일 뷰에서 접수 마감 상태 공고 카드의 내용과 타임라인 진행 바 사이에 불필요하게 넓은 공백이 노출됨.
* **원인 (Cause)**: 데스크톱용 마우스 호버 툴팁 안전 마진인 `padding-top: var(--spacing-md)` 속성이 툴팁이 동작하지 않는 모바일 화면에도 무조건 적용됨.
* **해결 (Solution)**: 모바일 미디어 쿼리에서 `padding-top`을 `0`으로 제거하고 하단 마진을 `var(--spacing-xs)`로 촘촘하게 줄여 가독성을 확보함.

### [이슈 날짜: 2026-07-08] 모바일 바텀시트 터치 드래그 먹통 및 UI 가변 규격 이탈 현상
* **분류**: 프론트엔드
* **현상 (Problem)**: 모바일 바텀시트 목록 영역 드래그 시 높이 조절이 전혀 작동하지 않고, 필터 모달의 왼쪽 텍스트와 전역 팝업이 화면 밖으로 탈출해 잘려 보임.
* **원인 (Cause)**: 하위 리스트 컴포넌트들의 터치 핸들러가 누락되어 제스처가 소실되었고, 디자인 토큰 연산 및 반응형 가로 폭 제어가 모바일 화면 크기를 초과했음.
* **해결 (Solution)**: 하위 리스트에 `{...touchHandlers}`를 복구하고 드래그 임계값 미만인 단순 클릭 시 콜백 실행을 방어했으며, 모달 최대 해상도와 필터 가로폭(264px)을 모바일에 맞게 정밀 축소함.

### [이슈 날짜: 2026-07-08] 모바일 바텀시트 최대 높이 드래그 시 리스트 스크롤 잠김 및 시트 축소 현상
* **분류**: 프론트엔드
* **현상 (Problem)**: 최대 높이 바텀시트에서 리스트 스크롤을 올리려 쓸어내릴 때 스크롤바가 잠기고 시트 전체가 먼저 축소됨.
* **원인 (Cause)**: 터치 시작 시 리스트 자식 요소의 높이를 측정하여 기준 높이가 어긋나고, 스크롤바가 공중에 있을 때의 스크롤 우선 리턴 분기가 부재함.
* **해결 (Solution)**: closest 메서드로 최상위 시트 높이 측정을 일원화하고, scrollTop > 0 및 최대높이 쓸어올림 시 스크롤을 즉시 양보 리턴하는 로직을 공통 훅에 추가함.

### [이슈 날짜: 2026-07-08] 모바일 바텀시트 터치 스크롤 동작 방해 및 지도 간섭 현상
* **분류**: 프론트엔드
* **현상 (Problem)**: 모바일 브라우저에서 바텀시트 내부 공고 리스트 스크롤이 되지 않거나 지도가 멋대로 움직임
* **원인 (Cause)**: 부모 래퍼(`.app-sidebar`)에 설정한 `pointer-events: none`이 자식 시트의 터치 무브 이벤트를 패스스루 시켜버림
* **해결 (Solution)**: 부모 크기를 바텀시트 면적으로 제한하고 normal `pointer-events: auto`로 전환하여 터치 입력을 정상 수신함

### [이슈 날짜: 2026-07-08] WSL2 개발 환경 외부 접속 시 React 하이드레이션 멈춤(Stall) 현상
* **분류**: 인프라(Next.js 배포)
* **현상 (Problem)**: 모바일 실기기로 WSL2 개발 서버(`npm run dev`) 외부 IP 접속 시 페이지 동작이 멈춤
* **원인 (Cause)**: Turbopack HMR 웹소켓 포트 차단 및 루프 실패로 인해 React 하이드레이션 과정이 일시 정지(Stall)됨
* **해결 (Solution)**: 외부 실기기 검증 시에는 HMR 소켓이 없는 프로덕션 빌드(`npm run build && npm run start`) 모드로 구동해 우회함

### [이슈 날짜: 2026-07-08] 모바일 웹 뷰포트 높이 가림 및 하단 탭바 잘림 현상
* **분류**: 프론트엔드
* **현상 (Problem)**: 모바일 크롬/사파리 브라우저의 하단 주소창 및 도구바에 의해 네비게이션바 하단 영역이 가려지거나 잘림
* **원인 (Cause)**: CSS 높이 단위에 고정형 `100vh`를 사용하여 가변형 모바일 뷰포트 영역을 정상 추적하지 못함
* **해결 (Solution)**: 디자인 토큰 및 CSS 높이 규격 단위를 동적 뷰포트 높이 단위인 `100dvh`로 전면 교체하여 해결함

### [이슈 날짜: 2026-07-07] 다중 폴더 상태 마이그레이션 시 세터 함수 참조 에러
* **분류**: 프론트엔드
* **현상 (Problem)**: 다중 폴더 개편 후 탭 전환 시 `setActiveFolderId is not defined` 에러 발생함.
* **원인 (Cause)**: 단일 상태를 배열 상태(`activeFolderIds`)로 바꾸면서 `handleTabSelect` 내 미수정 참조가 잔재함.
* **해결 (Solution)**: `setActiveFolderId(null)`을 `setActiveFolderIds([])`로 교체하여 에러를 해소함.

### [이슈 날짜: 2026-07-07] 드래그 앤 드롭 이동 후 단지 카드 흐림(투명도) 잔상 현상
* **분류**: 프론트엔드
* **현상 (Problem)**: 단지 카드를 타 폴더로 드롭한 뒤 해당 폴더를 열면 카드가 흐릿하게 보이는 현상.
* **원인 (Cause)**: 드롭 시 DOM 재배치로 인해 원본 엘리먼트가 소실되어 `onDragEnd` 이벤트가 브라우저 단에서 누락됨.
* **해결 (Solution)**: `onDrop` 핸들러 내부에서 `setDraggingComplexId(null)`를 강제 호출하여 드래그 상태를 해제함.

### [이슈 날짜: 2026-07-06] Nginx 프록시 환경의 Server Action CSRF 거부 및 정적 경로 불일치
* **분류**: 인프라(Next.js 배포)
* **현상 (Problem)**: 도메인 접속 시 Failed to find Server Action 에러가 발생하며 서버 액션 기능이 먹통이 됨.
* **원인 (Cause)**: Nginx 설정 내 X-Forwarded-Proto 헤더 누락으로 프로토콜이 불일치하여 CSRF 차단이 발생하고, static alias가 구버전 경로로 지정됨.
* **해결 (Solution)**: Nginx 설정에 X-Forwarded-Proto $scheme 헤더를 추가하고, static alias 경로를 실제 배포 경로(web)로 교정하여 해결함.

### [이슈 날짜: 2026-07-03] 프론트엔드 UI 스타일 파편화 및 하드코딩 토큰 척결
* **분류**: 프론트엔드
* **현상 (Problem)**: 각 컴포넌트별로 공통 UI(즐겨찾기, 모달 백드롭, 슬라이더 등) 스타일이 파편화 정의되고 하드코딩 단위가 잔류하여 유지보수성이 저하됨
* **원인 (Cause)**: 스타일을 전역화하지 않고 개별 모듈 CSS로 중복 선언하거나, 비즈니스 및 레이아웃 상수를 로컬에 다중 정의했기 때문임
* **해결 (Solution)**: 공통 스타일을 globals.css 및 src/constants로 이관하고 인라인 스타일을 모듈 CSS로 정리했으며, 하드코딩 수치는 디자인 토큰 배율 연산식으로 일괄 치환함

### [이슈 날짜: 2026-07-03] 인라인 CSS 변수에 var() 참조 전달 시 calc() 전체 무효화
* **분류**: 프론트엔드
* **현상 (Problem)**: 상세 패널이 사이드 패널 우측에 틈새 없이 달라붙어 렌더링됨
* **원인 (Cause)**: React 인라인 스타일로 CSS 변수(`--sidebar-offset-width`)에 `'var(--layout-sidebar-width)'` 문자열을 전달하면 브라우저가 Invalid로 처리해 `calc()` 수식 전체가 무효화됨
* **해결 (Solution)**: 변수 참조 문자열 대신 실제 수치 리터럴(`${SIDEBAR_DEFAULT_WIDTH}px`)을 직접 주입하도록 `page.tsx` 수정함

### [이슈 날짜: 2026-07-03] JSX 중복 style 속성으로 인한 Next.js 빌드 타입 오류
* **분류**: 인프라(Next.js 배포)
* **현상 (Problem)**: `npm run build:pack` 실행 시 `JSX elements cannot have multiple attributes with the same name` 타입 오류로 빌드 실패함
* **원인 (Cause)**: `page.tsx` 내 `DetailPanel` 컴포넌트에 `style` 속성이 인라인으로 두 번 중복 선언되었기 때문임
* **해결 (Solution)**: 중복된 `style` 속성을 하나로 병합하고 레이아웃 left 연산을 CSS 모듈 내 변수 수식으로 이관함

### [이슈 날짜: 2026-07-02] 저장 탭 대조 비교 화면의 formatMoney ReferenceError
* **분류**: 프론트엔드
* **현상 (Problem)**: 저장 탭에서 대조 비교 테이블을 렌더링할 때 브라우저에서 'formatMoney is not defined' 참조 오류가 발생하며 렌더링이 멈춤
* **원인 (Cause)**: `Sidebar.tsx` 파일 내에 금액 변환에 필수적인 `formatMoney` 및 `formatRent` 유틸리티 함수의 import 문이 누락됨
* **해결 (Solution)**: `Sidebar.tsx` 상단에 `@/utils/formatters`로부터 해당 포매터 함수들을 정상적으로 import 하도록 구문을 추가함

### [이슈 날짜: 2026-07-02] 감액 전환만 가능한 임대 조건의 보증금 상호 전환 슬라이더 누락
* **분류**: 프론트엔드
* **현상 (Problem)**: 감액 전환(보증금 낮추고 월세 추가 납부)만 가능한 주택형에서 전환 모의 계산 슬라이더가 렌더링되지 않고 전환 불가 안내가 노출됨
* **원인 (Cause)**: `hasConversion` 판별 조건에서 보증금 증액 가능 여부(`max_deposit > deposit`)만 필수 검증하여 감액 전환만 가능한 분기를 누락함
* **해결 (Solution)**: 판별 조건식에 감액 가능 여부(`min_deposit < deposit`) 조건을 OR 연산으로 추가 보완하여 정상 활성화함

### [이슈 날짜: 2026-06-30] HTML5 슬라이더 틱 마크 정렬 및 렌더링 위치 오차 해결
* **분류**: 프론트엔드
* **현상 (Problem)**: 슬라이더 틱 마크를 단순 백분율(left: percent%)로만 지정 시 동그라미(Thumb)의 중심 위치와 맞물리지 않고 어긋나는 오차 발생함
* **원인 (Cause)**: HTML5 슬라이더 동그라미가 트랙 양 끝에서 자체 반폭(radius)만큼 렌더링 여백을 갖기 때문임
* **해결 (Solution)**: `calc(반폭 + (100% - 동그라미너비) * (비율 / 100))` 수식을 적용해 틱 마크 위치를 정밀 보정하고, 초기값일 때 조건부 렌더링을 적용해 가려지게 함

### [이슈 날짜: 2026-06-30] Next.js Hydration Mismatch — 네이버 지도 스크립트 중복 로드
* **분류**: 프론트엔드
* **현상 (Problem)**: 브라우저 콘솔에 Hydration Mismatch 경고 발생, 서버와 클라이언트의 `<head>` 내 스크립트 속성 및 순서가 불일치함
* **원인 (Cause)**: `layout.tsx`의 일반 `<script>` 태그와 `Map.tsx`의 `next/script` 컴포넌트가 동일한 네이버 지도 URL을 이중 로드하여 DOM 노드 배치 충돌 발생함
* **해결 (Solution)**: `layout.tsx`의 중복 `<script>` 태그를 제거하고, `Map.tsx`의 `next/script`로 단일 로드 일원화 및 서브모듈 파라미터를 `geocoding` → `geocoder`로 오타 수정함

### [이슈 날짜: 2026-06-30] 접수 당일 접수예정 탭에 공고 잔류 및 D-Day 뱃지 소실
* **분류**: 프론트엔드
* **현상 (Problem)**: 접수 시작일 당일 오전 00:00~09:59 사이에 공고가 '접수예정' 탭에 잔류하고 D-Day 뱃지도 사라져 사용자 혼란 유발함
* **원인 (Cause)**: D-day 계산은 날짜 단위 비교, 접수 상태 판정은 정밀 시간 단위 비교로 로직 기준이 이원화되어 불일치 발생함
* **해결 (Solution)**: `getAnnouncementStatus` 함수를 날짜 단위 비교로 통일하여 접수 시작일 당일 0시부터 무조건 '접수중'으로 분류되도록 수정함


### [이슈 날짜: 2026-06-29] 애드센스 심사용 고가치 텍스트 부재 및 크롤링 봇 진입 제한
* **분류**: 프론트엔드
* **현상 (Problem)**: 메인 지도가 CSR(클라이언트 렌더링) 방식으로 동작하여 구글 봇이 1,000자 이상의 정보성 텍스트 공고 명세를 읽지 못하고 애드센스 심사가 거절됨
* **원인 (Cause)**: 단일 지도의 아코디언 컴포넌트 렌더링에 묶여있어 고유한 리소스 URI 주소(Path)와 SSR(서버 렌더링) 페이지 구조가 부재했기 때문임
* **해결 (Solution)**: `/announcements/details/[id]` 동적 라우팅을 개설하여 서버 사이드에서 직접 SQLite DB를 쿼리해 풀 텍스트 마크다운(MarkdownViewer) 및 조건 표 형태로 내려보내고, `sitemap.ts`를 동적 SQLite 연결 구조로 확장해 전체 URL을 검색 로봇에 색인 등록함

### [이슈 날짜: 2026-06-29] 글로벌 레이아웃 간섭으로 인한 상세 페이지 전체 스크롤바 억제 및 숨김
* **분류**: 프론트엔드
* **현상 (Problem)**: 상세 페이지(`/announcements/details/[id]`)에서 브라우저 화면 높이를 초과해도 세로 스크롤바가 생기지 않고 화면이 갇힘
* **원인 (Cause)**: 지도를 화면에 가득 채우는 글로벌 body 레이아웃 스타일(`globals.css`)의 `overflow: hidden;` 정책이 상세 페이지 뷰포트에 상속되었기 때문임
* **해결 (Solution)**: 상세 페이지 전용의 고정형 전체화면 스크롤 컨테이너를 정의한 `detail-layout.css` 및 `layout.tsx` 파일을 신설하여 글로벌 간섭을 완벽히 우회함

### [이슈 날짜: 2026-06-29] 딥링크 유입 후 화면 상태와 URL 간의 데이터 모순 및 롤백 부작용
* **분류**: 프론트엔드
* **현상 (Problem)**: 메인 페이지로 딥링크(`?announcement_id=ID`) 유입된 후 쿼리를 지우거나 다른 아코디언을 눌렀을 때, 주소창의 쿼리 상태가 화면의 아코디언 개폐 상태와 동기화되지 못하거나 새로고침 시 롤백됨
* **원인 (Cause)**: 메인 렌더링 상태와 브라우저 주소창(History State) 간에 양방향 실시간 동기화 플로우가 존재하지 않았기 때문임
* **해결 (Solution)**: 사용자가 아코디언을 토글하거나 클릭할 때마다 HTML5 replaceState를 통해 주소창 쿼리를 실시간 shallow push 동기화(선택 A)하여 모순 없는 유기적 경험을 구현함

### [이슈 날짜: 2026-06-29] 지도 활성화 마커 강조 색상의 시각적 피드백 오류
* **분류**: 프론트엔드
* **현상 (Problem)**: 주택 카드나 마커 클릭 시 활성 강조색이 초록색(SH 브랜드 컬러)으로 변경되어 화면 톤이 매끄럽지 않음
* **원인 (Cause)**: globals.css 내 활성 마커(.custom-marker.active)의 배경색 및 내부 링 색상이 var(--color-sh)로 고정 지정되어 있었음
* **해결 (Solution)**: 활성 상태 마커의 배경과 중앙 홀 fill 색상을 묵직하고 차분한 적갈색(#8a3028)으로 일괄 교정함

### [이슈 날짜: 2026-06-29] 민간 공고 뱃지 색상이 GH 하늘색 뱃지와 중복 렌더링되는 현상
* **분류**: 프론트엔드
* **현상 (Problem)**: 공급 기관이 '민간'인 경우에도 공고 카드 및 전용 뱃지 색상이 GH 하늘색으로 오도되어 출력됨
* **원인 (Cause)**: Badge.tsx와 AnnouncementCard.tsx의 삼항 분기에서 SH/LH/HUG가 아닐 시 무조건 gh 클래스로 귀결되는 오류가 있었음
* **해결 (Solution)**: '경기' 또는 'GH' 텍스트 검증을 통해 gh를 거르고 그 외 민간/기타는 private 클래스 및 전용 골드 브라운 토큰으로 매핑함

### [이슈 날짜: 2026-06-29] 상세 아코디언 개폐 슬라이딩 모션 부재 및 뚝 끊김 현상
* **분류**: 프론트엔드
* **현상 (Problem)**: 상세 공고 카드 내 아코디언 개폐 시 스르륵 열리지 않고 툭툭 끊기며 조건부 렌더링 마운트/디스마운트만 발생함
* **원인 (Cause)**: Boolean 조건식 단편 조건부 렌더링에 묶여있어 DOM이 즉각 유실되므로 CSS height transition이 동작하지 못함
* **해결 (Solution)**: scrollHeight를 실시간 측정하여 height와 opacity를 동적 전환하는 AccordionSection 컴포넌트를 설계하고 SVG 아이콘 대신 1. 2. 3. 순차 넘버링을 적용함

### [이슈 날짜: 2026-06-29] 사이드바 분할 레이아웃 조절 드래그 랙 및 높이 계산 복잡성
* **분류**: 프론트엔드
* **현상 (Problem)**: 상세 보기와 주택 목록이 상하 분할되어 수직 조절 리사이저 바 조작 시 드래그 랙 및 시각적 모션 끊김이 생김
* **원인 (Cause)**: 두 영역의 가변 높이를 MouseMove 픽셀 좌표 이벤트로 실시간 수치 연산하여 반영하는 리사이징 로직 복잡성 때문임
* **해결 (Solution)**: 수직 리사이저 바와 하단 고정 토글을 없애고 공급 주택 목록 카드를 상세 카드 내부의 자식 아코디언(Children)으로 통합해 단일 세로 스크롤로 해결함


### [이슈 날짜: 2026-06-26] SQLite 데이터베이스 갱신 시 웹 서비스 미반영 현상
* **분류**: 인프라(Next.js 배포)
* **현상 (Problem)**: SCP로 DB 파일 교체 및 PM2 서비스 재시작 후에도 신규 데이터가 웹에 반영되지 않음
* **원인 (Cause)**: Next.js API가 정적으로 캐싱되어 있고 빌드 단계에서 위경도 마이그레이션이 발생하여 DB만 교체해선 반영 안 됨
* **해결 (Solution)**: DB 데이터 변경 시 로컬에서 재빌드(`npm run build:pack`) 후 전체 배포하도록 `DEPLOYMENT.md` 가이드를 보완함

### [이슈 날짜: 2026-06-25] Next.js 16 빌드 타입 오류 (serverActions 위치)
* **분류**: 인프라(Next.js 배포)
* **현상 (Problem)**: `npm run build:pack` 시 `serverActions does not exist in type NextConfig` 타입 에러 발생함
* **원인 (Cause)**: Next.js 버전(16.x) 타입 정의 상 `serverActions` 옵션은 최상위가 아닌 `experimental` 객체 하위에 선언되어야 함
* **해결 (Solution)**: `next.config.ts`에서 `serverActions` 블록을 `experimental` 내부로 이동시켜 타입 오류 해결함

### [이슈 날짜: 2026-06-25] 도메인 환경 Server Action 호출 거부 에러
* **분류**: 인프라(Next.js 배포)
* **현상 (Problem)**: IP 접속은 되나 도메인 접속 시 `Failed to find Server Action` 에러가 발생하며 서버 액션이 차단됨
* **원인 (Cause)**: Next.js 14+부터 Nginx 프록시 환경에서 Origin 헤더가 일치하지 않으면 보안(CSRF) 매커니즘이 요청을 거부함
* **해결 (Solution)**: `next.config.ts`의 `experimental.serverActions.allowedOrigins`에 서비스 도메인을 명시하여 해결함

### [이슈 날짜: 2026-06-25] Nginx 심볼릭 링크 깨짐 오류
* **분류**: 인프라(Next.js 배포)
* **현상 (Problem)**: `nginx -t` 테스트 시 sites-enabled의 심볼릭 링크 원본을 찾을 수 없다는 에러 발생함
* **원인 (Cause)**: nano 에디터로 sites-available에 원본 파일을 생성할 때 내용만 붙여넣고 저장을 누락함
* **해결 (Solution)**: 에디터로 다시 열어 설정 본문 붙여넣기 후 명시적으로 저장(Ctrl+O)하여 해결함

